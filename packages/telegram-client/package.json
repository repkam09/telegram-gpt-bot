{
  "name": "@hennos/telegram-client",
  "version": "1.0.0",
  "description": "Telegram bot client for AI agent",
  "main": "build/index.js",
  "scripts": {
    "build": "npm-run-all clean tsc",
    "clean": "rimraf ./build",
    "tsc": "tsc",
    "lint": "eslint src/ --ext .ts",
    "start": "node build/index.js",
    "dev": "npm run build && npm run start"
  },
  "dependencies": {
    "@hennos/core": "^1.0.0",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/telegraf": "^4.0.0"
  }
}