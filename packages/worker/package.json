{
  "name": "@hennos/worker",
  "version": "1.0.0",
  "description": "Temporal worker service for AI agent",
  "main": "build/index.js",
  "scripts": {
    "build": "npm-run-all clean tsc",
    "clean": "rimraf ./build",
    "tsc": "tsc",
    "lint": "eslint src/ --ext .ts",
    "start": "node build/index.js",
    "dev": "npm run build && npm run start"
  },
  "dependencies": {
    "@hennos/core": "^1.0.0"
  }
}