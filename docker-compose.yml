services:
  hennos:
    container_name: hennos
    restart: unless-stopped
    image: hennos-gpt:latest
    ports:
      - "16006:16006"
    volumes:
      - ".env:/app/hennos/.env"
      - "./data/:/app/hennos/data/"
